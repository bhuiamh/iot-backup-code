#ifndef GLOBAL_VARIABLES_H
#define GLOBAL_VARIABLES_H

#include <Arduino.h>

extern bool card_presented;  // Declare the global variable as extern
extern String cardUID;       // Declare the global variable as extern

#endif
